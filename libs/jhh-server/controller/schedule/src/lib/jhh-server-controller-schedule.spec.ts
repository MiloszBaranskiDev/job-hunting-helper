import { jhhServerControllerSchedule } from './jhh-server-controller-schedule';

describe('jhhServerControllerSchedule', () => {
  it('should work', () => {
    expect(jhhServerControllerSchedule()).toEqual(
      'jhh-server-controller-schedule'
    );
  });
});

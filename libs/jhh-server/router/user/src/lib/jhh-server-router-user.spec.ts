import { jhhServerRouterUser } from './jhh-server-router-user';

describe('jhhServerRouterUser', () => {
  it('should work', () => {
    expect(jhhServerRouterUser()).toEqual('jhh-server-router-user');
  });
});

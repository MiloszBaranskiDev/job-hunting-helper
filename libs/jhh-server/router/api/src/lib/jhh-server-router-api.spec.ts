import { jhhServerRouterApi } from './jhh-server-router-api';

describe('jhhServerRouterApi', () => {
  it('should work', () => {
    expect(jhhServerRouterApi()).toEqual('jhh-server-router-api');
  });
});
